# Initialize app package

# Note: Template initialization is called from server.py main() to ensure
# proper logging is configured first. Do not import templates here to avoid
# circular imports and premature initialization.
